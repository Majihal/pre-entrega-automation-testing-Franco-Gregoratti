[pytest]
addopts = -v --html=reports/report.html --self-contained-html
markers =
    get: Tests relacionados con peticiones GET
    post: Tests relacionados con peticiones POST
    put: Tests relacionados con peticiones PUT
    patch: Tests relacionados con peticiones PATCH
    delete: Tests relacionados con peticiones DELETE
